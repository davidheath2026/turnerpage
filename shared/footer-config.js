/* ============================================================
   TURNER PAGE — SITE-WIDE FOOTER / PROMISE CONFIG
   ============================================================
   This is the ONLY file that should ever contain the wording for:
     - the small "promise strip" repeated at the bottom of every
       screen in every lesson
     - the larger "Turner Page Promise" callout card used inline
       inside lessons (via TPKit.promiseCard())
 
   Edit the two strings below and every lesson that loads this file
   (all of them should) will update automatically next time the page
   is opened. Nothing else needs to change.
 
   Load this file BEFORE turner-page-kit.js in every lesson:
     <script src="shared/footer-config.js"></script>
     <script src="shared/turner-page-kit.js"></script>
============================================================ */
 
window.TP_FOOTER = {
  // Short label shown in small caps above the promise text.
  label: "The Turner Page Mission",
 
  // The mission wording itself. Plain text or simple HTML.
  // CURRENT WORDING — replace with the updated copy when ready.
  text: "To develop the next generation of trusted Data Migration professionals <br>through practical learning, sound judgement and real-world thinking."
};
 
